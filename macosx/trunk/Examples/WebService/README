
HOWTO:

1) Create key pairs

openssl dsaparam 2048 < /dev/urandom > dsaparam.data
openssl gendsa dsaparam.data -out Manifest.key
openssl dsa -in Manifest.key -pubout -out Manifest.pem

2) Sign Manifest.xml

rm Manifest.sig
openssl dgst -sha1 -binary < Manifest.xml | openssl dgst -dss1 -sign Manifest.key | openssl enc >> Manifest.sig

3) Don't forget to update Updater/Info.plist and Updater/Manifest.pem !

<key>EstEIDWebService</key>
<array>
  <string>file:///Users/ ... /WebService/Manifest.xml</string>
</array>